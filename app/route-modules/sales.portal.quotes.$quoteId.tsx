import {
  loader as supportQuoteLoader,
  action as supportQuoteAction,
  default as SupportQuoteDetailPage,
} from "./support.quote-detail";

export const loader = supportQuoteLoader;
export const action = supportQuoteAction;
export default SupportQuoteDetailPage;
